stack基本概念
概念：stack是一种先进后出的（First In Last Out）数据结构，
它只允许在一端进行插入和删除操作。这种数据结构通常用于实现递归、
表达式求值、函数调用等场景。只有顶端的元素才可以被外界使用，因此
栈不允许有遍历行为
提问：栈可以判断容器是否为空吗？可以 empty()函数接口
栈可以返回元素个数吗？可以 size()函数接口，在入栈的时候记录，不能拿出来计数


栈中进入数据为入栈  push
栈中出去数据为出栈  pop
可以理解为子弹弹匣